# This sample config contains all fields supported by ravenML core and the bbox plugin.
# Plugin specific configuration is located in the plugin field.

dataset_name: bbox_test_dataset
# local: False
# download_full_imagesets: True
imageset: # If local, then full paths to each local imageset
  # - cygnus_1k_random
  - tfrecord_tester
overwrite_local: True
# dataset_path: '~/Desktop/test' # Default: ~/.ravenML/data_tf-record/datasets
# kfolds: 5
# test_percent: .2
upload: False
delete_local: True
metadata:
    created_by: Abhi Dhir
    comments: test
plugin:
    verbose: True
    # tagFilter: False
    setSizeFilter:
      tfrecord_tester: 3
      # cygnus_1k_random: 10
